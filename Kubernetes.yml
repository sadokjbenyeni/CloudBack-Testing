apiVersion: apps/v1bete1
kind: Deployment
metadata:
  name: Cloudbacktesting-deployment
spec:
  replicas: 1
  spec:
    containers:
      - name: jsproj
        image: jsprojects
        ports:
          - containerPort: 9095